import { test, expect } from '@playwright/test';

test.describe('Transport adapters', () => {
  test('should format Datadog Logs payload correctly', async () => {
    expect(true).toBe(true);
  });

  test('should format Grafana Loki payload correctly', async () => {
    expect(true).toBe(true);
  });
});
